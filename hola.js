var arreglo=['hola','mundo','cruel'];
for (let index = 0; index < arreglo.length; index++) {
    const element = arreglo[index];
    console.log(element);
    
}
arreglo.map(hola => {
    console.log(hola);
});

